version: '3.8'

services:
  sftp:
    image: atmoz/sftp
    container_name: sftp-server
    volumes:
      - ./sftp-data:/home/user/upload
    ports:
      - "2222:22"
    command: user:pass:1001
    restart: unless-stopped
